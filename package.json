{
  "name": "@unguibus/system-service",
  "version": "0.1.0",
  "main": "src/index.ts",
  "devDependencies": {
    "bun-types": "latest"
  },
  "description": "Local daemon for agent process lifecycle, messaging, and runtime management",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test"
  }
}
